# ------------------------------------------------------------------------------
#              -= Arno's iptables firewall - SIP-VOIP plugin =-
# ------------------------------------------------------------------------------

# To actually enable this plugin make ENABLED=1:
# ------------------------------------------------------------------------------
ENABLED=0

# Specify here the UDP port(s) for SIP (default = 5060)
# If you use Asterisk VOIP (also) specify its port range here. 
# Ie. in case the RTP streams are from port 10000 to port 20000 use
# SIP_VOIP_PORTS="10000,20000"
# ------------------------------------------------------------------------------
SIP_VOIP_PORTS=""
